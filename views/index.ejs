<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link href="/css/styles.css" rel="stylesheet">
    <title>Quiz app</title>
</head>

<body>
    <%- include ('partials/navbar.ejs') %>

    <h1 class="text-center display-3 my-5">Quizzes</h1>
    <div class="container">
        <div class="row row-cols-lg-3 g-4">
            
            <% for(let i=0; i < quizzes.length; i++) { %>
                <div class="col">
                    <div class="card h-100">
                        <img src="/images/<%= quizzes[i].image %>" class="card-img-top " alt="card example" style="max-height: 200px;"/>
                        <div class="card-body p-4">
                            <h5 class="card-title"><%= quizzes[i].name %></h5>
                            <p class="card-text">
                                Total qns: <%= quizzes[i].total_questions %>, 
                                <% if (quizzes[i].total_marks == null) { quizzes[i].total_marks = 0 } %>
                                Total marks: <%= quizzes[i].total_marks %><br>
                                <%= quizzes[i].description %>
                            </p>
                        </div>
                        <div class="card-footer py-3">
                            <a href="/editQuiz/<%= quizzes[i].quizId %>" class="card-link">Edit Quiz</a>
                            <a href="/startQuiz/<%= quizzes[i].quizId %>/1" class="card-link">Start Quiz</a>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>


</body>

</html>